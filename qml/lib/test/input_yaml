---
Controller:
  basename:
  methods:   # arg1, arg2, ...., return value
    onItemSelected: [int, int, unit]
    setPaths: [ string list, unit]
    paths: [unit, string list]
    getFullPath: [unit, string]
  properties:
    hasData:
      type:   bool
      get:    isHasData
      notify: hasDataChanged
    descr:
      type:   string
      get:    getDescr
      notify: descChanged
  slots:

AbstractModel:
  basename:  QAbstractItemModel
  methods:
  properties:
  slots:

DataItem:
  methods:
    name: [unit, string]
    sort: [unit, string]
  properties:
  slots:
