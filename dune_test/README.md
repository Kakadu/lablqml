dune-based demo with OCaml threads from stdlib Thread module.

The demo starts a background thread which updates GUI text every second.

    dune build program.exe 
    ../_build/default/dune_test/program.exe

